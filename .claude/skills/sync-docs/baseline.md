# sync-docs baseline

The last commit at which the `docs/` manual was verified against the
implementation. The `sync-docs` skill diffs `HEAD` against this commit to find
what changed, then updates this file. **Committed to git on purpose** — the
baseline must persist across sessions and machines.

- **Baseline commit:** `49235800`
- **Date:** 2026-06-07
- **Note:** Initial docs set created and verified against source; baseline starts here.
